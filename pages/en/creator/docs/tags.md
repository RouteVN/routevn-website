---
template: documentation
title: Tags
seo:
  title: "Tags | RouteVN Creator Docs"
  description: "Tags are labels that help you group related resources without moving them between folders. One item can have multiple tags, and one tag can be used by."
sidebarId: tags
---

Tags are labels that help you group related resources without moving them between folders. One item can have multiple tags, and one tag can be used by many items.

## Tag scopes

Tags are available across resource areas, but each area has its own tag list. For example, image tags are separate from sound tags.

Character sprite tags are also separate for each character.

## Assigning tags

Tags can be assigned to resource items, but not folders.

To assign tags to an item:

1. Select an item to display the right detail panel.
2. Click the tags area or `Add tag`.
3. Select existing tags that you want to assign, or create a new tag.
4. Press submit.

Some notes:

- Tags cannot have the same name.
- You can change the name of tags.
- An item can have as many tags as you assign.

## Filtering by tags

Use the tag filter in the page header to show items with selected tags.

When you select multiple tags, RouteVN Creator shows items that match any selected tag.

## Character Sprite Tags

For character sprites, tags are also used to create [sprite groups](/en/creator/docs/characters/#sprite-groups).
